
export interface Project {
  id: string;
  title: string;
  description: string;
  url: string;
  icon: string;
  tags: string[];
  color: string;
}

export interface AIInsight {
  quote: string;
  advice: string;
}
